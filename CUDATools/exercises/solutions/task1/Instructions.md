# Task 1: `cuda-memcheck`

Find the error in the `set_vector.cu` program using `cuda-memcheck`.

* Compile with `make`
* Launch a batch job with `make run`
* Launch a debug batch job with `make memcheck`
